{
  "buildCommand": "node scripts/generate-env.js && npm run build -- --configuration production",
  "outputDirectory": "dist/angular/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}