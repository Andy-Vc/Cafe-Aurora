{
  "buildCommand": "echo \"export const environment = { production: true, apiUrl: '$API_URL', supabaseUrl: '$SUPABASE_URL', supabaseAnonKey: '$SUPABASE_ANON_KEY' };\" > src/environments/environment.prod.ts && npm run build -- --configuration production",
  "outputDirectory": "dist/cafe-aurora/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}